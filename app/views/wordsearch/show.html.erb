<%= render(Primer::Alpha::Banner.new(icon: :info, dismiss_scheme: :hide, classes: "wordsearch-banner")) { "Drag over the words in the wordsearch to find them!" } %>
<br>
<div class="wordsearch-container"
     data-controller="wordsearch"
     data-wordsearch-id="<%= @wordsearch.id %>">
     <div class="wordsearch-complete-message">
     🎉 Wordsearch Complete! 🎉
     </div>
     <div class="spinner">
          <%= render(Primer::Beta::Spinner.new(size: :medium)) %>
     </div>
</div>